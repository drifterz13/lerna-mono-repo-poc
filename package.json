{
  "name": "lerna-mono-repo-poc",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "test": "pnpm -r --parallel --stream run test",
    "build": "pnpm -r --parallel --stream run build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {},
  "devDependencies": {
    "lerna": "^8.0.1"
  }
}
